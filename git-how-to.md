 HEAD
# GitHub Setup Instructions

## 1. Как создать SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"

## 2. Как добавить ключ в аккаунт на GitHub
cat ~/.ssh/id_ed25519.pub
# скопировать и вставить на GitHub → Settings → SSH and GPG keys → New SSH key

## 3. Как склонировать репозиторий
git clone git@github.com:username/Fafurin.git

# 🚀 Полная инструкция: SSH ключ + GitHub + Клонирование

## 📋 Что делаем по шагам:
1. ✅ Создаём SSH ключ на компьютере
2. ✅ Добавляем ключ в аккаунт GitHub
3. ✅ Клонируем репозиторий через SSH

---

## 🔐 ШАГ 1: СОЗДАНИЕ SSH КЛЮЧА

### Открываем терминал и пишем:

```bash
# 1. Генерируем ключ (Ed25519 - современный и безопасный)
ssh-keygen -t ed25519 -C "ваш_real_email@example.com"

# 2. На вопросы отвечаем так:
#    • "Enter file in which to save the key" → просто жмём Enter
#    • "Enter passphrase" → придумываем пароль ИЛИ оставляем пустым (удобнее без пароля)
#    • "Enter same passphrase again" → повторяем пароль

# 3. Добавляем ключ в ssh-agent (чтобы не вводить пароль каждый раз)
eval "$(ssh-agent -s)"          # запускаем агент
ssh-add ~/.ssh/id_ed25519       # добавляем ключ

# 4. Проверяем, что ключ создан:
ls -la ~/.ssh/id_ed25519*       # должны увидеть 2 файла



🌐 2. Добавление ключа в GitHub
Шаг 2.1: Копирование публичного ключа
На macOS:

text
pbcopy < ~/.ssh/id_ed25519.pub
На Linux (если установлен xclip):

text
xclip -sel clip < ~/.ssh/id_ed25519.pub
Если не сработало: просто выведите ключ и скопируйте вручную:

text
cat ~/.ssh/id_ed25519.pub
Копируйте всю строку (начинается с ssh-ed25519 AAAAC3...)

Шаг 2.2: Добавление на GitHub
Зайдите на github.com и авторизуйтесь

Нажмите на аватар (правый верх) → Settings

В левом меню выберите SSH and GPG keys

Нажмите New SSH key (зелёная кнопка)

Заполните форму:

Title: Придумайте название (например: "Мой ноутбук")

Key type: Оставьте "Authentication Key"

Key: Вставьте скопированный ключ

Нажмите Add SSH key → введите пароль от GitHub если потребуется

📥 3. Клонирование репозитория
Шаг 3.1: Подготовка папки
Сначала перейдите в папку, где хотите хранить проекты:

text
cd ~/Desktop
Или создайте специальную папку:

text
mkdir -p ~/Projects
cd ~/Projects
Шаг 3.2: Копирование SSH+ссылки
Откройте нужный репозиторий на GitHub

Нажмите зелёную кнопку Code

Выберите вкладку SSH

Скопируйте ссылку (формат: git@github.com:username/repo.git)

Шаг 3.3: Клонирование
В терминале выполните:

text
git clone git@github.com:username/repository.git
Например:

text
git clone git@github.com:octocat/Hello-World.git
Шаг 3.4: Переход в проект
text
cd repository_name
Теперь вы в папке проекта и можете работать!

✅ 4. Проверка работоспособности
Проверка SSH подключения к GitHub:
text
ssh -T git@github.com
Успешный ответ:

text
Hi username! You've successfully authenticated, but GitHub does not provide shell access.
Если ошибка "Permission denied":

Проверьте, что добавили правильный ключ на GitHub

Попробуйте перезагрузить ssh-agent:

text
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
Проверка Git настроек:
text
git config --global user.name
git config --global user.email
Если ничего не выводит, настройте:

text
git config --global user.name "Ваше Имя"
git config --global user.email "ваш_email@example.com"
🛠️ 5. Решение частых проблем
🔴 Проблема: "Permission denied (publickey)"
Решение:

text
# 1. Проверьте права файлов
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub

# 2. Перезапустите ssh-agent
killall ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 3. Проверьте, что ключ добавлен на GitHub
ssh-add -l
🔴 Проблема: Клонирование через SSH просит пароль
Решение: Вы использовали HTTPS ссылку вместо SSH

На GitHub нажмите Code → выберите SSH → скопируйте правильную ссылку

Или поменяйте remote у существующего репозитория:

text
git remote set-url origin git@github.com:username/repo.git
🔴 Проблема: Несколько аккаунтов GitHub
Создайте файл ~/.ssh/config:

text
# Личный аккаунт
Host github.com-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal

# Рабочий аккаунт
Host github.com-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
Используйте так:

text
git clone git@github.com-personal:username/repo.git
git clone git@github.com-work:company/repo.git
💡 Полезные советы
Храните приватный ключ в безопасности — никогда не отправляйте файл id_ed25519 кому-либо

Публичный ключ можно показывать — файл id_ed25519.pub безопасен для публикации

Один ключ — один компьютер — для каждого устройства создавайте новый ключ

Используйте понятные названия при добавлении ключа на GitHub

Регулярно проверяйте список ключей в настройках GitHub и удаляйте неиспользуемые

🎯 Краткая шпаргалка
text
# Создать ключ:
ssh-keygen -t ed25519 -C "email"

# Добавить в агент:
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Скопировать ключ:
pbcopy < ~/.ssh/id_ed25519.pub

# Проверить подключение:
ssh -T git@github.com

# Клонировать репозиторий:
git clone git@github.com:username/repo.git

# Настроить Git:
git config --global user.name "Имя"
git config --global user.email "email"
surname

